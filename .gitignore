# Ignore all dotfiles...
.*

# Ignore all pyc file
*.pyc

# except for .gitignore and .gitkeep
!.gitignore
!.gitkeep

# also don't ignore Gitpod files
!.gitpod.Dockerfile
!.gitpod.yml

backup

Dropbox

# ignore files uploaded to the site to save space
media
media-original
media-download
# but not theme files
!themes/*/media
# ignore any node_modules directories
node_modules
themes/*/node_modules

# ignore private settings
secrets.json
credentials

# ignore generated static files
static
static-old

whoosh_index/*
!whoosh_index/README
