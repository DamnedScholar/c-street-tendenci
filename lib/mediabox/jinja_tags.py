from django_jinja import library

# TODO: Brainstorm filter and component tags to go in this file. `jinja_ext.py` exposes the standard Mediabox API as a global, and the reusable Mediabox brain shouldn't depend on/know about how to render a Jinja template. `jinja_tags.py` is the ideal place for logic that transforms API output into markup.
