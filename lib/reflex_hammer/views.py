from django.shortcuts import render
from django.urls import resolve


def hammer(path=''):
    # Given a URL path, `resolve()` it and display information about the resolved path as well as creating an object that will receive updates about reflexes that happen at that URL path
    pass
